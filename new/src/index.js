import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';



ReactDOM.render( //функция которая выводит на экран приложения то что внутри нее собственно
  <App />,  // получается, что мы в апп нашаршим, то оно и покажет
  document.getElementById("root")
);